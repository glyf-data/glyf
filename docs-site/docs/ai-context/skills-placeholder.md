# Skills Placeholder

This page reserves space for downloadable or copyable instructions that help users integrate dbt-charts with coding assistants such as Codex or Claude.

## Future Codex skill

The future skill should help an agent:

- Read dbt project structure and manifest artifacts.
- Generate `.ggsql` visualisations using supported syntax.
- Update dashboard YAML.
- Run `dbt-charts doctor`, `validate`, `render`, and `dashboard`.
- Explain failures in terms of dbt artifacts, chart syntax, or DuckDB execution.

## Future Claude project instructions

The future project instruction template should tell Claude:

- Where chart files live.
- Which commands validate changes.
- Which chart types and directives are supported.
- How to avoid changing dbt models unless asked.

## Placeholder install text

```text
Coming soon: copy this skill into your agent workspace to let AI generate and validate dbt-charts dashboards from your dbt project.
```
