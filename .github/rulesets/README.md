# Repository rulesets

`protect-main.json` is the versioned copy of the ruleset applied to `main`:
pull requests only, no force-pushes or deletion, review threads resolved before
merge. Committing a change here does not apply it; the maintainer applies it
through the GitHub API or the repository settings.
