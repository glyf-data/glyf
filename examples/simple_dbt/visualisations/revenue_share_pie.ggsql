SELECT region, sum(revenue) as revenue
FROM {{ ref('fct_orders') }}
GROUP BY 1

VISUALISE region AS x, revenue AS y
DRAW pie
LABEL title => 'Revenue Share'
LABEL subtitle => 'Share of revenue by region'
