SELECT department, sum(gross_margin) as gross_margin
FROM {{ ref('fct_finance') }}
GROUP BY 1
ORDER BY gross_margin DESC

VISUALISE department AS x, gross_margin AS y
DRAW pie
LABEL title => 'Gross Margin Share'
LABEL y_title => 'Gross Margin'
INTERACT tooltip
